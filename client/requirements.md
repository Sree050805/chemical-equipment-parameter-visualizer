## Packages
recharts | Data visualization for charts
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes
date-fns | Date formatting for history and charts

## Notes
Authentication uses the provided blueprint pattern with /api/register, /api/login, /api/user.
File uploads use multipart/form-data via POST /api/datasets/upload.
Charts require responsive containers from Recharts.
